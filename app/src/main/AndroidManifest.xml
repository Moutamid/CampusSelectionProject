<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moutamid.campusselectionproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampusSelectionProject">
        <activity android:name=".RegistrationNumbersActivity">

        </activity>
        <activity android:name=".EditCompaniesActivity" />
        <activity android:name=".EditStudentsActivity" />
        <activity android:name=".QueryActivity" />
        <activity android:name=".UploadPostVacancyActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".CompanyActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ReportActivity" />
        <activity android:name=".ActivityLogin" />
        <activity android:name=".ActivitySignUp"></activity>
        <activity android:name=".UploadCvActivity" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>